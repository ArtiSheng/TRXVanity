# 旧网页源码归档

[English](README.md)

此目录只用于保留旧网页文件，不参与原生 macOS App 的构建或运行。

- `app/page.tsx`：旧网页的页面结构（JSX/HTML）
- `app/layout.tsx`：旧网页的 HTML 外层布局
- `app/globals.css`：旧网页的完整 CSS

旧 Worker、网页加密代码及历史构建配置也原样归档在本目录，仅供查阅。
